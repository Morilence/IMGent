name: imgent

services:
  imgent:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ./data:/app/data
      - ./workspace:/workspaces/main
    environment:
      IMGENT_QQ_APP_ID: ${IMGENT_QQ_APP_ID:-}
