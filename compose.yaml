services:
  agent-pigeon:
    build: .
    restart: unless-stopped
    ports:
      - "127.0.0.1:8787:8787"
    volumes:
      - ./data:/app/data
      - ./workspace:/workspaces/main
    environment:
      AGENT_PIGEON_QQ_APP_ID: ${AGENT_PIGEON_QQ_APP_ID:-}
